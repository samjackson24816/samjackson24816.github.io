

Write in markdown.  Keep the names of all the files the same (duplicate an existing project folder and edit the contents)
You can embed files, but you have to use html syntax.  You can reference files directly, ("/file") if they are in the same directory as the md file.

Run "node build.js" to update the compiled json file and add your new changes to the website.